VERSION := 14.2.0
GCC_NAME := gcc-$(VERSION)
export GCC_NAME
TAR := $(GCC_NAME).tar.gz
MIRROR := https://ftp.gnu.org/gnu/gcc/$(GCC_NAME)/$(TAR)

COMPLETE := build.complete

$(COMPLETE):
	echo "Definitely building GCC"
